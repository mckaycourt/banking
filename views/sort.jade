extends layout

block content
    link(rel='stylesheet', href='/stylesheets/sort.css')
    script(src='https://code.jquery.com/jquery-3.3.1.js')
    .categories
        .category
            | Food
        .category
            | Rent
        .category
            | Utilities
        .category
            | Gas
        .category
            | Entertainment
        .category
            | Personal
        .category
            | Education
    .items
        each item in grouped
            .item(draggable="true" id=item.Description.replace(/\s/g, '').replace('#',''))= item.Description
    script(src='/scripts/sort.js')